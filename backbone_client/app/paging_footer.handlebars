<div>
  <span class="cell">
    {{#if morePages}}
    <a href="#" class="nextinfinitepagination"><span>{{locale_More}}</span></a>
    {{/if}}
    <span class="cell howmanyperpagination">
        <span>{{locale_Show}}</span>
      <a href="#" class="selected">10</a>      |
      <a href="#"  >50</a>
     |
      <a href="#"  >100</a>
       <span>{{locale_per_page}}</span>
    </span>
    <span class="divider">/</span>
    <span class="cell first records">
      <span class="current">{{currentPage}}</span>

 <span>{{locale_of}}</span>
      <span class="total">{{totalPages}}</span>
  <span>{{locale_pages_shown}}</span>
      {{#if allShown}}
        (All)
      {{/if}}
    </span>
  </span>
</div>