define([ "use!backbone", "corpus/Corpus" ],
    function(Backbone, Corpus) {
      var CorpusView = Backbone.View.extend(
      /** @lends CorpusView.prototype */
      {
        /**
         * @class This is the corpus view. TBA 
         * 
         * @extends Backbone.View
         * @constructs
         */
       

      });

      return CorpusView;
    });